/**
* interface Strategy has no constructor
* and has one abstract method hitOrStand(); no implementations.
**/
public interface Strategy {

	// abstract hitOrStand method, pass in integer, and has no implementations
	public boolean hitOrStand(int sum);
	
} // end Strategy interface
